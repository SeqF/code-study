package sort;

/**
 * 计数排序
 *
 * @author paksu
 */
public class CountingSort {

    /**
     * 计数排序其实是桶排序的一种特殊情况。当要排序的n个数据所处的范围并不大时，比如最大值是K，就可以
     * 把数据划分为K个桶，每个桶内的数据都是相同的，省掉了桶内排序的时间
     * <p>
     * 时间复杂度为O（n），即线性排序，是非基于比较的排序算法
     */
    public void countingSort() {

    }
}
